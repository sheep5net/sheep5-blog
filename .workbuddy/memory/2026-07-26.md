# 2026-07-26 工作日志

## Hugo + PaperMod 博客搭建

在 `/Users/kuangwencheng/Desktop/sheep5/` 搭建了 Hugo 博客：
- Hugo v0.164.0 extended（已预装）
- PaperMod 主题（git submodule 方式引入）
- 配置了中文站、搜索、归档、标签菜单
- 创建了示例文章 `content/posts/hello-world.md`
- 固定链接 `/posts/5位随机数字/`，archetype 自动生成 slug
- 关于页面 `/about/`
- 全站中文翻译、日期格式 `2006年01月02日`
- RSS `/rss.xml`（最多5篇），sitemap `/sitemap.xml`（daily）
- 站点图标 favicon/apple-touch-icon
- SEO 描述 + 社交媒体分享图 `og-image.jpg`（1200x400，去 EXIF）
- 已 git init + 首次提交
- 构建测试通过
- 待用户操作：创建 GitHub 仓库并推送，然后在 Cloudflare Pages 配置部署

### 部署信息
- 域名：https://sheep5.net
- 部署方式：GitHub + Cloudflare Pages
- GitHub 仓库名：sheep5-blog
- SSH key 已有（~/.ssh/id_ed25519），但尚未添加到 GitHub
- 构建命令：`hugo --gc --minify`，输出目录 `public`
- Hugo 版本：v0.164.0 extended
- 待用户完成：Step1 添加SSH key → Step2 创建GitHub仓库 → Step3 推送代码 → Step4 CF Pages部署
- 代码已提交两次 commit，工作区干净

## SEO 检查与修复

修复了以下问题：
1. 文章页缺少 `og:image` 和 `twitter:image` → archetype 加 `images = ['og-image.jpg']`
2. 文章页 `twitter:card` 从 `summary` 改为 `summary_large_image`
3. `meta name=author` 为空 → hugo.toml 加 `author = "Sheep5"`
4. "Posts"/"Tags"/"Categories" 英文标题 → 创建 `_index.md` 设中文标题
5. 面包屑 "Posts" → "文章"
6. `safari-pinned-tab.svg` 缺失 → 创建 SVG
7. RSS 误含"关于"页 → `mainSections = ["posts"]` + about 页 `build.list: never`
8. `og:image:width/height/type/alt` 通过 `layouts/partials/extend_head.html` 添加
9. 归档页月份 "July" → "7月"，覆盖 `layouts/archives.html`
